# Some plan

This is a legacy plan artifact under `specs/plans/`. The migration tool should move it to `.workflow/plans/some-plan.md`.

The contents are intentionally minimal — the test cares about the move, not the contents.
