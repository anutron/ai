# Some design — brainstorm

This is a legacy brainstorm artifact under `specs/docs/<date>-<slug>/`. The migration tool should move the entire dated folder to `.workflow/docs/2026-01-01-some-design/`.

The contents are intentionally minimal.
