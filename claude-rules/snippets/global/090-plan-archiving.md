---
tags: [spec]
audience: [shared]
---
<!--
This snippet was removed during the OpenSpec migration. In OpenSpec, plans
live as `tasks.md` inside `openspec/changes/<name>/`, and `openspec archive
<name>` cleans them up at the end of a change. Legacy plans archived during
migration are at `.workflow/plans/` for historical reference.
-->
